(function () {
  const KEY = 'tractform_map_proto_v3';
  const FT_AC = 43560;
  const EIGHTH_SIDE = Math.sqrt(FT_AC / 8);
  const YARD = 3;
  const USES = {
    homesite: { id:'homesite', name:'Homesite', short:'House', color:'#064e3b', wiki:'/land.html', topic:'Land', min:0.5, ok:1, w:80, h:80, detail:'House, drive, septic setbacks.' },
    garden: { id:'garden', name:'Kitchen garden', short:'Garden', color:'#65a30d', wiki:'/gardens.html', topic:'Gardens', min:0.05, ok:0.25, w:50, h:40, detail:'Beds near the house.' },
    orchard: { id:'orchard', name:'Orchard', short:'Orchard', color:'#a16207', wiki:'/gardens.html', topic:'Gardens', min:0.15, ok:0.5, w:90, h:70, detail:'Home orchard wants about 0.5 ac.' },
    poultry: { id:'poultry', name:'Poultry yard', short:'Poultry', color:'#d97706', wiki:'/production.html', topic:'Livestock', min:0.05, ok:0.2, w:40, h:40, detail:'Backyard flock footprint.' },
    pasture: { id:'pasture', name:'Pasture', short:'Pasture', color:'#4d7c0f', wiki:'/production.html', topic:'Livestock', min:2, ok:8, w:200, h:160, detail:'Pair of sheep or 1-2 cattle wants ~2 ac.' },
    hay: { id:'hay', name:'Hay field', short:'Hay', color:'#ca8a04', wiki:'/land.html', topic:'Land', min:2, ok:5, w:180, h:140, detail:'Winter feed starts around 2 ac.' },
    woodlot: { id:'woodlot', name:'Woodlot', short:'Woods', color:'#166534', wiki:'/land.html', topic:'Land', min:1, ok:5, w:140, h:140, detail:'Not extra usable pasture.' },
    barn: { id:'barn', name:'Barn / shop', short:'Barn', color:'#7c2d12', wiki:'/buildings.html', topic:'Buildings', min:0.05, ok:0.15, w:50, h:36, detail:'Footprint only.' },
    well: { id:'well', name:'Well / septic', short:'Well', color:'#0369a1', wiki:'/utilities.html', topic:'Utilities', min:0.02, ok:0.08, w:28, h:28, detail:'Demand only. No setbacks yet.' },
    access: { id:'access', name:'Drive / lane', short:'Lane', color:'#78716c', wiki:'/land.html', topic:'Land', min:0.1, ok:0.25, w:120, h:18, detail:'Access and setbacks.' }
  };
  const state = { targetAc:5, shape:'square', verts:[], stamps:[], focus:{kind:'parcel',vert:0}, scale:1, ox:0, oy:0, drag:null };
  const svg = document.getElementById('stage');
  function uid(){ return 's'+Math.random().toString(36).slice(2,8); }
  function shoelace(verts){ let a=0; for(let i=0;i<verts.length;i++){ const p=verts[i], q=verts[(i+1)%verts.length]; a+=p.x*q.y-q.x*p.y; } return Math.abs(a)/2; }
  function rectVerts(x,y,w,h){ return [{x:x,y:y},{x:x+w,y:y},{x:x+w,y:y+h},{x:x,y:y+h}]; }
  function rectFor(acres,shape){ const area=acres*FT_AC; const ratio=shape==='wide'?2:shape==='deep'?0.5:1; const w=Math.sqrt(area*ratio); const h=area/w; return rectVerts(0,0,w,h); }
  function bounds(verts){ let minX=Infinity,minY=Infinity,maxX=-Infinity,maxY=-Infinity; verts.forEach(p=>{ minX=Math.min(minX,p.x); minY=Math.min(minY,p.y); maxX=Math.max(maxX,p.x); maxY=Math.max(maxY,p.y); }); return {minX,minY,maxX,maxY,w:maxX-minX,h:maxY-minY}; }
  function centroid(verts){ let x=0,y=0; verts.forEach(p=>{x+=p.x;y+=p.y;}); return {x:x/verts.length,y:y/verts.length}; }
  function scaleVerts(verts,factor,origin){ const o=origin||centroid(verts); return verts.map(p=>({x:o.x+(p.x-o.x)*factor,y:o.y+(p.y-o.y)*factor})); }
  function translate(verts,dx,dy){ return verts.map(p=>({x:p.x+dx,y:p.y+dy})); }
  function normalizeStamp(s){ if(s.verts&&s.verts.length>=3) return s; if(s.w!=null){ s.verts=rectVerts(s.x||0,s.y||0,s.w,s.h); delete s.x; delete s.y; delete s.w; delete s.h; } return s; }
  function stampVerts(s){ return normalizeStamp(s).verts; }
  function activeVerts(){ if(state.focus.kind==='stamp'){ const s=state.stamps.find(x=>x.id===state.focus.id); return s?stampVerts(s):state.verts; } return state.verts; }
  function setActiveVerts(next){ if(state.focus.kind==='stamp'){ const s=state.stamps.find(x=>x.id===state.focus.id); if(s) s.verts=next; } else state.verts=next; }
  function longestEdge(verts){ let best=0,bestD=-1; for(let i=0;i<verts.length;i++){ const a=verts[i], b=verts[(i+1)%verts.length]; const d=(a.x-b.x)**2+(a.y-b.y)**2; if(d>bestD){ bestD=d; best=i; } } return best; }
  function insertNode(verts,after){ const i=after==null?longestEdge(verts):after; const a=verts[i], b=verts[(i+1)%verts.length]; const next=verts.slice(); next.splice(i+1,0,{x:(a.x+b.x)/2,y:(a.y+b.y)/2}); return next; }
  function removableIndex(verts,preferred){ if(verts.length<=3) return -1; if(preferred!=null&&preferred>=0&&preferred<verts.length) return preferred; let best=0,bestScore=Infinity; for(let i=0;i<verts.length;i++){ const a=verts[(i+verts.length-1)%verts.length], b=verts[i], c=verts[(i+1)%verts.length]; const score=Math.hypot(b.x-a.x,b.y-a.y)+Math.hypot(c.x-b.x,c.y-b.y); if(score<bestScore){ bestScore=score; best=i; } } return best; }
  function deleteNode(verts,idx){ if(verts.length<=3) return verts; const i=removableIndex(verts,idx); const next=verts.slice(); next.splice(i,1); return next; }
  function setNodeCount(n){ n=Math.max(3,Math.min(48,Math.round(n))); let verts=activeVerts().slice(); while(verts.length<n) verts=insertNode(verts); while(verts.length>n) verts=deleteNode(verts); setActiveVerts(verts); state.focus.vert=Math.min(state.focus.vert||0,verts.length-1); syncSpin(); draw(); }
  function syncSpin(){ const spin=document.getElementById('node-spin'); const label=document.getElementById('node-target'); const verts=activeVerts(); if(spin&&document.activeElement!==spin) spin.value=verts.length; if(label){ if(state.focus.kind==='stamp'){ const s=state.stamps.find(x=>x.id===state.focus.id); label.textContent=s?'Editing '+USES[s.use].short+' outline':'Editing parcel outline'; } else label.textContent='Editing parcel outline'; } }
  function fit(){ const b=bounds(state.verts); const r=svg.getBoundingClientRect(); const pad=48; const sx=(r.width-pad*2)/Math.max(40,b.w); const sy=(r.height-pad*2)/Math.max(40,b.h); state.scale=Math.max(0.02,Math.min(sx,sy)); state.ox=pad-b.minX*state.scale; state.oy=pad-b.minY*state.scale; }
  function toWorld(clientX,clientY){ const r=svg.getBoundingClientRect(); return {x:(clientX-r.left-state.ox)/state.scale,y:(clientY-r.top-state.oy)/state.scale}; }
  function inside(pt,verts){ let odd=false; for(let i=0,j=verts.length-1;i<verts.length;j=i++){ const a=verts[i], b=verts[j]; if((a.y>pt.y)!==(b.y>pt.y)&&pt.x<(b.x-a.x)*(pt.y-a.y)/(b.y-a.y)+a.x) odd=!odd; } return odd; }
  function dist2(a,b){ const dx=a.x-b.x, dy=a.y-b.y; return dx*dx+dy*dy; }
  function distToSeg(p,a,b){ const vx=b.x-a.x, vy=b.y-a.y; const len2=vx*vx+vy*vy||1; let t=((p.x-a.x)*vx+(p.y-a.y)*vy)/len2; t=Math.max(0,Math.min(1,t)); const q={x:a.x+t*vx,y:a.y+t*vy}; return {t,q,d:Math.sqrt(dist2(p,q))}; }
  function gridStepFt(){ const want=56/state.scale; const steps=[YARD,9,18,36,EIGHTH_SIDE/2,EIGHTH_SIDE,EIGHTH_SIDE*2,EIGHTH_SIDE*4]; for(let i=0;i<steps.length;i++) if(steps[i]>=want) return steps[i]; return steps[steps.length-1]; }
  function gridLabel(step){ if(Math.abs(step-YARD)<0.05) return '1 yard'; if(Math.abs(step-9)<0.1) return '3 yards'; if(Math.abs(step-EIGHTH_SIDE)<0.5) return '1/8 acre square'; if(step<EIGHTH_SIDE) return Math.round(step)+' ft'; const ac=(step*step)/FT_AC; return ac>=0.12?ac.toFixed(2)+' ac cells':Math.round(step)+' ft'; }
  function stampAreaAc(s){ return +(shoelace(stampVerts(s))/FT_AC).toFixed(3); }
  function load(){ try{ const raw=JSON.parse(localStorage.getItem(KEY)||localStorage.getItem('tractform_map_proto_v2')||'null'); if(!raw) return false; if(raw.targetAc) state.targetAc=raw.targetAc; if(raw.shape) state.shape=raw.shape; if(Array.isArray(raw.verts)&&raw.verts.length>=3) state.verts=raw.verts; if(Array.isArray(raw.stamps)) state.stamps=raw.stamps.map(normalizeStamp); return !!state.verts.length; } catch(e){ return false; } }
  function save(){ localStorage.setItem(KEY, JSON.stringify({targetAc:state.targetAc,shape:state.shape,verts:state.verts,stamps:state.stamps})); }
  function renderPalette(){ document.getElementById('palette').innerHTML=Object.values(USES).map(u=>'<div class="chip flex items-center gap-2 rounded-lg border border-stone-200 px-2 py-1.5 bg-white" draggable="true" data-id="'+u.id+'"><span class="w-3.5 h-3.5 rounded-sm shrink-0" style="background:'+u.color+'"></span><span class="text-xs font-medium">'+u.short+'</span></div>').join(''); document.querySelectorAll('#palette .chip').forEach(el=>{ el.addEventListener('dragstart', ev=>{ ev.dataTransfer.setData('text/plain', el.dataset.id); ev.dataTransfer.effectAllowed='copy'; }); }); }
  function issuesFor(id,acres){ const u=USES[id]; if(acres+1e-9<u.min) return {level:'short',text:acres+' ac is under the usual minimum ('+u.min+' ac) for '+u.name.toLowerCase()+'.'}; if(acres+1e-9<u.ok) return {level:'tight',text:acres+' ac is a tight '+u.name.toLowerCase()+' (comfortable ~'+u.ok+' ac).'}; return {level:'ok',text:acres+' ac covers a basic '+u.name.toLowerCase()+'.'}; }
  function renderVerdict(){ const drawnAc=+(shoelace(state.verts)/FT_AC).toFixed(2); const drift=+(drawnAc-state.targetAc).toFixed(2); document.getElementById('area-meta').textContent='Drawn area '+drawnAc+' ac · target '+state.targetAc+' ac'+(Math.abs(drift)>=0.1?' · '+(drift>0?'+':'')+drift+' ac vs target':''); const used={}; state.stamps.forEach(s=>{ used[s.use]=+((used[s.use]||0)+stampAreaAc(s)).toFixed(3); }); const list=[]; if(Math.abs(drift)>=0.25) list.push({level:'tight',text:'Shape area is '+drawnAc+' ac, not the '+state.targetAc+' ac target.'}); if(!state.stamps.length) list.push({level:'unset',text:'No uses placed yet.'}); if(!used.homesite&&state.stamps.length) list.push({level:'tight',text:'No homesite on the tract.'}); if(used.pasture&&used.pasture<2) list.push({level:'short',text:'Pasture is only '+used.pasture+' ac.'}); state.stamps.forEach(s=>{ if(!inside(centroid(stampVerts(s)), state.verts)) list.push({level:'short',text:USES[s.use].short+' sits outside the parcel boundary.'}); }); const worst=list.find(x=>x.level==='short')||list.find(x=>x.level==='tight')||list[0]; document.getElementById('verdict-head').textContent=worst?worst.text:'Layout looks workable.'; document.getElementById('verdict-detail').textContent='Topics stay generic. This page flags fit and sends you there.'; document.getElementById('verdict-list').innerHTML=Object.keys(used).map(id=>{ const bad=issuesFor(id,used[id]).level==='short'; return '<li class="flex justify-between gap-2"><span>'+USES[id].short+'</span><span class="tabular-nums '+(bad?'text-amber-300':'text-emerald-100')+'">'+used[id]+' ac</span></li>'; }).join(''); document.getElementById('zoom-meta').textContent='Grid: '+gridLabel(gridStepFt())+' · '+(state.scale*3).toFixed(1)+' px / yard'; syncSpin(); }
  function inspectStamp(s){ const box=document.getElementById('inspector'); if(!s){ box.classList.add('hidden'); return; } const u=USES[s.use]; const ac=stampAreaAc(s); const flag=issuesFor(s.use,ac); document.getElementById('ins-name').textContent=u.name; document.getElementById('ins-acres').textContent=ac+' ac · '+stampVerts(s).length+' nodes'; const scaleEl=document.getElementById('ins-scale'); if(document.activeElement!==scaleEl) scaleEl.value=ac; document.getElementById('ins-note').textContent=u.detail+' Drag corners to reshape, or scale with the acres field.'; const link=document.getElementById('ins-link'); link.href=u.wiki; link.textContent='Open '+u.topic+' topic →'; const cls=flag.level==='short'?'bg-amber-50 text-amber-900 border-amber-200':flag.level==='tight'?'bg-stone-50 text-stone-700 border-stone-200':'bg-emerald-50 text-emerald-900 border-emerald-200'; document.getElementById('ins-flags').innerHTML='<p class="text-xs leading-relaxed border rounded-lg px-3 py-2 '+cls+'">'+flag.text+'</p>'; box.classList.remove('hidden'); }
  function polyAttr(verts){ return verts.map(p=>(state.ox+p.x*state.scale)+','+(state.oy+p.y*state.scale)).join(' '); }
  function drawHandles(verts,kind,id){ const selected=state.focus.kind===kind&&(kind==='parcel'||state.focus.id===id); const nodes=verts.map((p,i)=>{ const cx=state.ox+p.x*state.scale, cy=state.oy+p.y*state.scale; const on=selected&&state.focus.vert===i; return '<circle data-kind="'+kind+'" data-id="'+(id||'')+'" data-vert="'+i+'" cx="'+cx+'" cy="'+cy+'" r="'+(on?8:6)+'" fill="#fff" stroke="'+(on?'#d97706':'#065f46')+'" stroke-width="2"/>'; }).join(''); if(!selected) return nodes; const mids=verts.map((p,i)=>{ const q=verts[(i+1)%verts.length]; const mx=state.ox+(p.x+q.x)/2*state.scale; const my=state.oy+(p.y+q.y)/2*state.scale; return '<circle data-kind="'+kind+'" data-id="'+(id||'')+'" data-edge="'+i+'" cx="'+mx+'" cy="'+my+'" r="5" fill="#a7f3d0" stroke="#065f46" stroke-width="1"/>'; }).join(''); return mids+nodes; }
  function draw(){ const r=svg.getBoundingClientRect(); const step=gridStepFt(); const b=bounds(state.verts); const pad=400; const x0=Math.floor((b.minX-pad)/step)*step; const y0=Math.floor((b.minY-pad)/step)*step; const x1=b.maxX+pad; const y1=b.maxY+pad; let grid=''; for(let x=x0;x<=x1;x+=step){ const sx=state.ox+x*state.scale; grid+='<line x1="'+sx+'" y1="0" x2="'+sx+'" y2="'+r.height+'" stroke="#d6d3d1" stroke-width="1"/>'; } for(let y=y0;y<=y1;y+=step){ const sy=state.oy+y*state.scale; grid+='<line x1="0" y1="'+sy+'" x2="'+r.width+'" y2="'+sy+'" stroke="#d6d3d1" stroke-width="1"/>'; } const stamps=state.stamps.map(s=>{ const u=USES[s.use]; const verts=stampVerts(s); const on=state.focus.kind==='stamp'&&state.focus.id===s.id; const c=centroid(verts); return '<g data-stamp="'+s.id+'"><polygon points="'+polyAttr(verts)+'" fill="'+u.color+'" fill-opacity="0.5" stroke="'+(on?'#fbbf24':u.color)+'" stroke-width="'+(on?3:1.5)+'"/><text x="'+(state.ox+c.x*state.scale)+'" y="'+(state.oy+c.y*state.scale)+'" font-size="11" fill="#fff" text-anchor="middle" font-family="Inter,system-ui">'+u.short+'</text></g>'; }).join(''); let handles=drawHandles(state.verts,'parcel',''); const focusedStamp=state.focus.kind==='stamp'?state.stamps.find(s=>s.id===state.focus.id):null; if(focusedStamp) handles+=drawHandles(stampVerts(focusedStamp),'stamp',focusedStamp.id); svg.innerHTML='<rect width="100%" height="100%" fill="#f5f5f4"/>'+grid+'<polygon points="'+polyAttr(state.verts)+'" fill="#ecfdf5" fill-opacity="0.7" stroke="#065f46" stroke-width="2"/>'+stamps+handles; renderVerdict(); save(); }
  function hitVertOn(verts,w,pxTol){ const tol=pxTol/state.scale; for(let i=0;i<verts.length;i++){ if(Math.sqrt(dist2(w,verts[i]))<=tol) return i; } return -1; }
  function hitEdgeOn(verts,w,pxTol){ const tol=pxTol/state.scale; for(let i=0;i<verts.length;i++){ const a=verts[i], b=verts[(i+1)%verts.length]; const s=distToSeg(w,a,b); if(s.d<=tol&&s.t>0.12&&s.t<0.88) return i; } return -1; }
  function hitStamp(w){ for(let i=state.stamps.length-1;i>=0;i--){ if(inside(w,stampVerts(state.stamps[i]))) return state.stamps[i]; } return null; }
  svg.addEventListener('wheel', ev=>{ ev.preventDefault(); const w=toWorld(ev.clientX,ev.clientY); const factor=ev.deltaY<0?1.12:1/1.12; const next=Math.max(0.02,Math.min(8,state.scale*factor)); state.ox=ev.clientX-svg.getBoundingClientRect().left-w.x*next; state.oy=ev.clientY-svg.getBoundingClientRect().top-w.y*next; state.scale=next; draw(); }, {passive:false});
  svg.addEventListener('pointerdown', ev=>{ if(ev.button!==0) return; const w=toWorld(ev.clientX,ev.clientY); if(state.focus.kind==='stamp'){ const s=state.stamps.find(x=>x.id===state.focus.id); if(s){ const edge=hitEdgeOn(stampVerts(s),w,12); if(edge>=0){ s.verts=insertNode(stampVerts(s),edge); state.focus.vert=edge+1; inspectStamp(s); draw(); return; } const vi=hitVertOn(stampVerts(s),w,12); if(vi>=0){ state.focus.vert=vi; state.drag={kind:'stamp-vert',id:s.id,i:vi}; svg.setPointerCapture(ev.pointerId); inspectStamp(s); draw(); return; } } } const pEdge=hitEdgeOn(state.verts,w,10); if(state.focus.kind==='parcel'&&pEdge>=0){ state.verts=insertNode(state.verts,pEdge); state.focus={kind:'parcel',vert:pEdge+1}; inspectStamp(null); draw(); return; } const pVert=hitVertOn(state.verts,w,12); if(pVert>=0){ state.focus={kind:'parcel',vert:pVert}; state.drag={kind:'parcel-vert',i:pVert}; inspectStamp(null); svg.setPointerCapture(ev.pointerId); draw(); return; } const st=hitStamp(w); if(st){ state.focus={kind:'stamp',id:st.id,vert:0}; state.drag={kind:'stamp-move',id:st.id,x:w.x,y:w.y}; inspectStamp(st); svg.setPointerCapture(ev.pointerId); draw(); return; } state.focus={kind:'parcel',vert:state.focus.vert||0}; inspectStamp(null); state.drag={kind:'pan',x:ev.clientX,y:ev.clientY,ox:state.ox,oy:state.oy}; svg.classList.add('drag'); svg.setPointerCapture(ev.pointerId); draw(); });
  svg.addEventListener('pointermove', ev=>{ if(!state.drag) return; if(state.drag.kind==='pan'){ state.ox=state.drag.ox+(ev.clientX-state.drag.x); state.oy=state.drag.oy+(ev.clientY-state.drag.y); draw(); return; } const w=toWorld(ev.clientX,ev.clientY); if(state.drag.kind==='parcel-vert'){ state.verts[state.drag.i]={x:w.x,y:w.y}; draw(); } else if(state.drag.kind==='stamp-vert'){ const s=state.stamps.find(x=>x.id===state.drag.id); if(s){ s.verts[state.drag.i]={x:w.x,y:w.y}; inspectStamp(s); draw(); } } else if(state.drag.kind==='stamp-move'){ const s=state.stamps.find(x=>x.id===state.drag.id); if(s){ const dx=w.x-state.drag.x, dy=w.y-state.drag.y; s.verts=translate(stampVerts(s),dx,dy); state.drag.x=w.x; state.drag.y=w.y; inspectStamp(s); draw(); } } });
  svg.addEventListener('pointerup', ()=>{ state.drag=null; svg.classList.remove('drag'); });
  svg.addEventListener('dragover', ev=>ev.preventDefault());
  svg.addEventListener('drop', ev=>{ ev.preventDefault(); const id=ev.dataTransfer.getData('text/plain'); if(!USES[id]) return; const w=toWorld(ev.clientX,ev.clientY); const u=USES[id]; const s={id:uid(),use:id,verts:rectVerts(w.x-u.w/2,w.y-u.h/2,u.w,u.h)}; state.stamps.push(s); state.focus={kind:'stamp',id:s.id,vert:0}; inspectStamp(s); draw(); });
  document.getElementById('acres').addEventListener('change', ()=>{ const n=Number(document.getElementById('acres').value); state.targetAc=Number.isFinite(n)&&n>0?n:5; state.verts=rectFor(state.targetAc,state.shape); state.focus={kind:'parcel',vert:0}; fit(); draw(); });
  document.getElementById('shape-btns').addEventListener('click', ev=>{ const btn=ev.target.closest('[data-shape]'); if(!btn) return; state.shape=btn.dataset.shape; document.querySelectorAll('.shape-btn').forEach(b=>{ const on=b.dataset.shape===state.shape; b.className='shape-btn px-2.5 py-1.5 rounded-md border text-xs font-medium '+(on?'border-emerald-800 bg-emerald-800 text-white':'border-stone-300 bg-white'); }); state.verts=rectFor(state.targetAc,state.shape); state.focus={kind:'parcel',vert:0}; fit(); draw(); });
  document.getElementById('fit-btn').addEventListener('click', ()=>{ fit(); draw(); });
  document.getElementById('clear-uses').addEventListener('click', ()=>{ state.stamps=[]; state.focus={kind:'parcel',vert:0}; inspectStamp(null); draw(); });
  document.getElementById('add-node').addEventListener('click', ()=>{ setActiveVerts(insertNode(activeVerts(), state.focus.vert)); state.focus.vert=Math.min((state.focus.vert||0)+1, activeVerts().length-1); const s=state.focus.kind==='stamp'?state.stamps.find(x=>x.id===state.focus.id):null; if(s) inspectStamp(s); draw(); });
  document.getElementById('del-node').addEventListener('click', ()=>{ setActiveVerts(deleteNode(activeVerts(), state.focus.vert)); state.focus.vert=Math.min(state.focus.vert||0, activeVerts().length-1); const s=state.focus.kind==='stamp'?state.stamps.find(x=>x.id===state.focus.id):null; if(s) inspectStamp(s); draw(); });
  document.getElementById('node-spin').addEventListener('change', ()=>{ setNodeCount(Number(document.getElementById('node-spin').value)); const s=state.focus.kind==='stamp'?state.stamps.find(x=>x.id===state.focus.id):null; if(s) inspectStamp(s); });
  document.getElementById('ins-remove').addEventListener('click', ()=>{ if(state.focus.kind==='stamp') state.stamps=state.stamps.filter(s=>s.id!==state.focus.id); state.focus={kind:'parcel',vert:0}; inspectStamp(null); draw(); });
  document.getElementById('ins-scale').addEventListener('change', ()=>{ if(state.focus.kind!=='stamp') return; const s=state.stamps.find(x=>x.id===state.focus.id); if(!s) return; const nextAc=Number(document.getElementById('ins-scale').value); const cur=stampAreaAc(s); if(!Number.isFinite(nextAc)||nextAc<=0||cur<=0) return; s.verts=scaleVerts(stampVerts(s), Math.sqrt(nextAc/cur)); inspectStamp(s); draw(); });
  renderPalette();
  const restored=load();
  document.getElementById('acres').value=state.targetAc;
  document.querySelectorAll('.shape-btn').forEach(b=>{ const on=b.dataset.shape===state.shape; b.className='shape-btn px-2.5 py-1.5 rounded-md border text-xs font-medium '+(on?'border-emerald-800 bg-emerald-800 text-white':'border-stone-300 bg-white'); });
  if(!restored) state.verts=rectFor(state.targetAc,state.shape);
  requestAnimationFrame(()=>{ fit(); draw(); });
  window.addEventListener('resize', ()=>draw());
})();
